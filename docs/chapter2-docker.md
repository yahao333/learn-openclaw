# Docker 安装（可选）

> 一般不需要用 Docker，直接用上面的方法安装即可！

如果你需要用 Docker：

```bash
docker run -it -v ~/.openclaw:/root/.openclaw openclaw/openclaw
```

---

**上一章**：[安装 OpenClaw](./chapter2-installation.md)
**下一章**：[配置文件详解](./chapter3-configuration.md)
